---
const { surface = 'light', background = "", type = "column" } = Astro.props;
const wrapperClass = type === "column" ? "content-wrapper container mx-auto"
                   : type === "full"   ? "full-bleed"
                   : "content-wrapper container mx-auto";
---

<section class="content-full" data-surface={surface} style={`background-image: url(${background});`}>
    <div class={wrapperClass}>
        <slot />
    </div>
</section>

<style>
    .content-full {
        padding: var(--gap-full) 0;
        background-position: top center;
        background-position-x: 35px;
        background-position-y: 7px;
    }

    .content-wrapper {
        margin: 0 auto;
    }

    .content-wrapper :global(h4) {
        margin-bottom: var(--gap-half);
    }

    .full-bleed {
        overflow-x: auto;
        width: 100%;
    }

    .full-bleed::-webkit-scrollbar {
        display: none;
    }

    @media (max-width: 80em) {
        .content-wrapper {
            padding: 30px;
        }
    }
</style>